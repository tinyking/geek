---
const projects = [
  {
    title: "tk-obsidian-flomo",
    desc: "Obsidian 轻量级笔记捕获与回顾插件，灵感来自 Flomo 工作流：快速捕获、自然打标签、在清晰的时间线上回顾思考。",
    tag: "🔧 开源项目",
    icon: "📦",
    href: "https://github.com/tinyking/tk-obsidian-flomo",
  },
];
---

<section id="projects" class="section-padding">
  <div class="text-center mb-16">
    <span class="text-[#E57035] text-xs font-bold tracking-widest uppercase">
      精选作品
    </span>
    <h2 class="text-4xl font-semibold mt-3">最近的项目</h2>
  </div>
  <div class="grid gap-6">
    {projects.map((p, i) => (
      <a
        key={i}
        href={p.href}
        target="_blank"
        rel="noopener noreferrer"
        class="group bg-white border border-[#E5E5E0] p-8 rounded-xl flex flex-col md:flex-row gap-8 transition-all hover:shadow-lg hover:border-[#9CA3AF]"
      >
        <div class="w-full md:w-48 h-36 bg-gradient-to-br from-[#FFF5F0] to-[#FFEDE5] rounded-lg flex items-center justify-center text-4xl">
          {p.icon}
        </div>
        <div class="flex-1">
          <h3 class="text-xl font-semibold mb-2">{p.title}</h3>
          <p class="text-[#6B6B6B] mb-4">{p.desc}</p>
          <div class="flex gap-4 text-xs text-[#9CA3AF]">
            <span>{p.tag}</span>
            {p.href !== "#" && <span>🔗 GitHub</span>}
          </div>
        </div>
      </a>
    ))}
  </div>
</section>
